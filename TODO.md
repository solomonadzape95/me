## TODO

- [ ] Last.fm: create an API key (https://www.last.fm/api/account/create) and add `LASTFM_API_KEY` + `LASTFM_USERNAME` to `.env.local`. Pair Spotify→Last.fm in your Last.fm account so scrobbles flow.
- [ ] Replace placeholder screenshots for the 9 new projects (currently reusing shots from lisk/envelope/jotter).
- [ ] Refresh original screenshots for lisk / envelope / jotter when you have cleaner hero frames.
- [ ] Add real logos for `flipit` and `gdg` (currently using the identicon fallback).
- [ ] Add an Open Graph PNG that matches the typography (current OG is `/content/images/file.jpg`).
- [ ] Consider tighter dates for the 9 new projects — most use month-1 of the year as a stand-in.
- [ ] Optional: extend the command palette with external destinations (resume.pdf direct, project repos).
- [ ] Optional: an `/uses` page.
- [ ] Try a different DiceBear avatar seed if the current `solenoid` face doesn't feel right.
